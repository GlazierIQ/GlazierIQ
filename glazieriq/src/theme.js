// GlazierIQ color system — single source of truth
// Change a color here, it changes everywhere.

export const theme = {
  // Foundation
  bg: '#1E1E1E',        // Graphite Black — app background
  text: '#C9CED3',      // Brushed Aluminum — body text

  // Brand
  brand: '#FFD400',     // Construction Yellow — buttons, highlights, active states
  brandDark: '#1E1E1E', // text color ON yellow buttons

  // Alerts
  alert: '#FF3B30',     // Signal Red — warnings, failed QC, storms, delete
  alertDark: '#2B0300', // text color ON red backgrounds

  // Safety module only
  safety: '#FF6A00',    // Safety Orange — Safety pages/badges only
  safetyDark: '#1E1E1E' // text color ON orange
}
